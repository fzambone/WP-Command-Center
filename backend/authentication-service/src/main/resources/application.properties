# Server port
server.port=4000

# Application name
spring.application.name=authentication-service

# Logging
logging.level.root=INFO
logging.level.com.wpcommandcenter=DEBUG

# Firebase configuration (you'll need to add your Firebase project details later)
# firebase.database-url=https://your-project-id.firebaseio.com
# firebase.credentials-file-path=/path/to/your/firebase-adminsdk.json